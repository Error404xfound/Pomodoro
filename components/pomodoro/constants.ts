/** Default focus session length in minutes. */
export const DEFAULT_FOCUS_LENGTH = 25;
/** Default break session length in minutes. */
export const DEFAULT_BREAK_LENGTH = 5;

/** Minimum supported session length in minutes. */
export const MIN_LENGTH = 1;
/** Maximum supported session length in minutes. */
export const MAX_LENGTH = 60;
